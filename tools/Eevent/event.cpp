#include "event.h"

using namespace esperantoEvent;
/*
  library test code

bool fa(int a) {
	fprintf(stderr, "1\n");
	if (a > 1) return true;
	else return false;
}

void fb(int a, int b) {
	fprintf(stderr, "add() %d + %d = %d\n", a, b, a+b);
	return;
}

int main() {
	Event e;
	e.addSignal(&fa, 10);
	e.addSlot(&fb, 3, 7);

	signal_func_t sig = e.getSignal();
	slot_func_t slot = e.getSlot();
	sig();
	slot();
	
	return 0;
}
*/
