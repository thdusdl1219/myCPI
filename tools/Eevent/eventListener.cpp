#include "eventListener.h"

using namespace esperantoEvent;

// EventListener eventListener; UNUSED

/*
void timer_handler(int signum) {
	typedef std::vector<Signal>::iterator S;
	for(S it = eventListener.listenerList.begin(), 
		ie = eventListener.listenerList.end(); it != ie; ++it) {
		Signal signal = *it;
		(signal.get())();
	} 
}

EventListener::EventListener() {
	this->install();
}

void EventListener::install() {
	timer.installTimer(&timer_handler);
}
*/
